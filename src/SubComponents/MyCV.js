import MyCV from "../assets/mohammad_bakibillah_chowdhuryy.pdf"
import React from "react";

const ReadCV = () => {
    return (
        <embed src={MyCV} width={window.innerWidth} height={window.innerHeight} />
    )
}

export default ReadCV